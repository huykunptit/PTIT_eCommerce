; Reduce baseline memory usage on small dynos (e.g., 512MB)
; Heroku PHP buildpack merges this into the php-fpm pool config.
pm = ondemand
pm.max_children = 2
pm.process_idle_timeout = 10s
pm.max_requests = 200
